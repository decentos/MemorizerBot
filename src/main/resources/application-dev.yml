spring:
  datasource:
    url: jdbc:mysql://localhost:3306/memorizer_bot?useUnicode=true&serverTimezone=UTC&useSSL=false
    username: root
    password: Consensus69
  sql:
    init:
      mode: always